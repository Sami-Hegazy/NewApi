<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.task2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_news"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_news"
        android:supportsRtl="true"
        android:theme="@style/Theme.Task2">
        <activity
            android:name=".ui.view.activities.SaveActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.activities.DetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".ui.view.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>